name: vue-web-frame 

on: 
  push:
    branches:
      - master

jobs:
  build:
    
    runs-on: ubuntu-latest
    steps:
    - name: Install and Build
      run: |
        npm i
        npm start
      
